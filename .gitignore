node_modules/
dist/
store/
data/
.env
.env.*
!.env.example
.DS_Store
*.log

# Runtime artifacts
.backups/
crm.db
*.tmp
*.db-journal

# Per-group runtime state (templates in crm/groups/ stay tracked)
groups/*/CLAUDE.md.tmp
groups/*/.crm-sessions/
groups/*/attachments/

# Claude Code per-project state
.claude/agent-memory/
.claude/settings.json
# (settings.local.json is also ignored via ~/.config/git/ignore — matches Claude Code convention)

# TypeScript incremental build cache (regenerated on every tsc run)
*.tsbuildinfo

# Aura KB raw import archive (content lives unpacked in aura-kb/)
aura-kb-import.zip

# taxonomy · vocabularios controlados

Cada archivo `.yaml` define una dimensión de etiquetado con sus valores permitidos.
El frontmatter de todo el KB sólo puede usar valores listados aquí. Si falta un valor,
se agrega aquí primero (control de versión = control de vocabulario).
